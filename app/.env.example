# Not a secret: a LAN address. Set to your laptop's IP so the phone can reach
# the server, or to a cloudflared tunnel URL if the venue wifi isolates clients.
#   echo "EXPO_PUBLIC_API_URL=http://$(ipconfig getifaddr en0):3100" > app/.env
EXPO_PUBLIC_API_URL=http://localhost:3100
